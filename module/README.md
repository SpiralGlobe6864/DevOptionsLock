DevOptionsLock root module

Install as a normal Magisk/KernelSU-compatible module.
The module daemon runs service.sh and monitors /data/adb/devoptionslock/state.
The companion app calls ctl.sh after validating its password.
